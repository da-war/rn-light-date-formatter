// src/index.d.ts

/**
 * Formats a date string into a more readable format.
 * @param date - The date to format, which can be a string or Date object.
 * @param format - The format to use (optional). Defaults to 'YYYY-MM-DD'.
 * @returns The formatted date string.
 */
export declare function formatDate(date: string | Date, format?: string): string;
